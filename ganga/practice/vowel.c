#include <stdio.h>

void main()
{
	char a, e, i, o, u, A, E, I, O, U;
	char ch = 'A';

	if(ch == 'a'|| ch == 'A' || ch == 'e'|| ch == 'E' || ch == 'i' || ch == 'I' || ch == 'o'|| ch == 'O' || ch == 'u' || ch =='U')
	{
		printf("vowel\n");
	}

	else
	{
		printf("consonant\n");
	}
}
