#include <stdio.h>
main()
{
	const int length =10;
	const int  width=10;
	const char NEWLINE ='\n';
	int area;
	area=length*width;
	printf("value of area:%d",area);
	printf(" %d",NEWLINE);
	return 0;
}
