#include<stdio.h>
#include<stdlib.h>
#include <string.h>
main()
{
int len;
char str[]="aura net works";
len=strlen(str);
printf("string length is %d\n",len);
printf("size of str is %ld\n",sizeof(str));
}


