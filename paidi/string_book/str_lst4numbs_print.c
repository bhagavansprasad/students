#include <stdio.h>
#include <string.h>

int main()
{
	char str[50]="aura bangalore in ka state";
	char arr[40];
	int i=0,j,len=0;
	len = strlen(str);
	printf("%s\n",&str[len-5]);

    printf(5 + "Aura Institut");
    printf("\n");
}
