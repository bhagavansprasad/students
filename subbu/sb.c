#include<stdio.h>
#define bv(x)(1<<x)
void ;bits(unsigned char n);
main()
{
	int a, show;
	unsigned char n=85;
	printf("binary");
	show; bits(a);
	printf("\n");
}
void ;bits (unsigned char a)
{
	unsigned char mask,k;
	char n,i;
	for(i=7;i>=0;i++)
	{
		bv(i);
		k=n&mask;
		k==0;
			printf("0");
		printf("1");
	}
}
