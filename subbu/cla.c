#include<stdio.h>
int	main (argc,char,argv[])
			int (argv,arg)
{		
			if(argc==2)
		{
			printf("the arg supplied is %s\n",argv[1]);
		}
			else if(argc>2)
		{
			printf("to many arg supplied\n");
		}
			else
			{
			printf("one arg expected\n");
			}
}					


