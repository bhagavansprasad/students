#include<stdio.h>
main()
{
	int len=20;
	char buff[520];
	int rval,wc,sum,get,fd,i,ptr;
	int get_wc(char*ptr);
	{
	int i;
	int wc=0;
	for( i=0;ptr[i]!='\0';i++);
	{
	if(ptr[i]=='')ptr[i]=='\n'&&ptr[i+1]!='  ';
	wc=wc++;
	}
	return(wc);
}	
