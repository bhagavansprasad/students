#include<stdio.h>
#include<sys/types.h>
#include<sys/static.h>
#include<fcntl.h>
#include<perrno.h>
int main()
{
int fd=0,char buff=[100],i=0;
fd=open(t.txt,0,0_rdonly);
if(fd=-1)
perror("%d",errno.h);
exit(n);
}

