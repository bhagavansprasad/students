#include<stdio.h>
	int main(int )
main()
{
	int i,c,n=25;
	for(i=2;i<=n;i++)
	{
		c=prime(i);
		printf("this is primr number=%d\n",c);
	}
}
int prime(int i)
{
	int t;
	for(t=2;t<=i-1;t++)
	{
		if (i%t==0)
		{
			break;
		}
		return t;
	}
}
