#include <stdio.h>
#define SIZE 30

main()
{
	int pid,fd,i;
	char msg[SIZE];
	char buff[SIZE];
	fd = open("raghu",O_RDWR);
	pid = fork();
	
	if(pid == 0)
	{
		
	}
	else
	{
		scanf("%s\n",msg[SIZE]);






	}
}
